import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    backgroundColor: "blue",
    height: 400 
  },
  title:{
    fontSize: 20,
    color: "gray"
  }
});

export { styles };
