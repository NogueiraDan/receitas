import { StyleSheet} from "react-native";

const styles = StyleSheet.create({
    container:{
        flexDirection:"row",
        padding: 8
    },
    text:{
        lineHeight: 20
    }
}) 


export {styles}