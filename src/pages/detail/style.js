import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    backgroundColor: "black",
    height: 400 
  },
  title:{
    fontSize: 20,
    color: "white"
  }
});

export { styles };
